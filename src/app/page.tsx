import Link from "next/link";
import { getCurrentUser } from "@/lib/auth";

export default async function Home() {
  const user = await getCurrentUser();
  return <main className="min-h-screen bg-[#f7f8fc] text-slate-900"><header className="border-b border-slate-200 bg-white"><div className="mx-auto flex h-16 max-w-7xl items-center justify-between px-5 sm:px-8"><div className="flex items-center gap-2.5 font-semibold tracking-tight"><span className="grid h-8 w-8 place-items-center rounded-lg bg-blue-600 text-xs text-white">CI</span>Conversation Intelligence</div><nav className="flex items-center gap-4"><Link href={user ? "/dashboard" : "/login"} className="text-sm font-semibold text-slate-600 hover:text-slate-950">{user ? "Dashboard" : "Log in"}</Link><Link href={user ? "/dashboard" : "/signup"} className="rounded-lg bg-blue-600 px-4 py-2 text-sm font-semibold text-white hover:bg-blue-700">{user ? "Open workspace" : "Get started"}</Link></nav></div></header><section className="mx-auto max-w-4xl px-5 py-24 text-center sm:px-8 sm:py-32"><p className="text-sm font-semibold tracking-[.14em] text-blue-700">CONVERSATION-TO-DATA</p><h1 className="mx-auto mt-5 max-w-3xl text-4xl font-semibold tracking-[-.045em] text-slate-950 sm:text-6xl">Turn customer conversations into <span className="text-blue-600">structured business data.</span></h1><p className="mx-auto mt-6 max-w-2xl text-lg leading-8 text-slate-600">Transcribe business calls, extract the information that matters, and keep reliable JSON in one private workspace.</p><div className="mt-9 flex justify-center gap-3"><Link href={user ? "/dashboard" : "/signup"} className="rounded-lg bg-blue-600 px-5 py-3 text-sm font-semibold text-white shadow-sm hover:bg-blue-700">{user ? "Open workspace" : "Get started"}</Link><Link href={user ? "/dashboard/schemas" : "/login"} className="rounded-lg border border-slate-300 bg-white px-5 py-3 text-sm font-semibold text-slate-700 hover:bg-slate-50">{user ? "Manage schemas" : "Log in"}</Link></div><div className="mx-auto mt-16 grid max-w-3xl gap-4 text-left sm:grid-cols-3"><div className="rounded-xl border border-slate-200 bg-white p-5"><p className="font-semibold">Upload</p><p className="mt-1 text-sm leading-6 text-slate-600">Bring common call recordings into a single workflow.</p></div><div className="rounded-xl border border-slate-200 bg-white p-5"><p className="font-semibold">Extract</p><p className="mt-1 text-sm leading-6 text-slate-600">Use the default schema or define your own fields.</p></div><div className="rounded-xl border border-slate-200 bg-white p-5"><p className="font-semibold">Return later</p><p className="mt-1 text-sm leading-6 text-slate-600">Keep transcripts and structured JSON available to your account.</p></div></div></section></main>;
}
